import {test,expect} from '@playwright/test';
import path from 'node:path';

const sunflower=path.resolve('tests/species-assets/sunflower.png');
async function clearDb(page){await page.goto('/observe.html');await page.evaluate(()=>new Promise(resolve=>{const r=indexedDB.deleteDatabase('herbarium.local.v1');r.onsuccess=r.onerror=r.onblocked=()=>resolve();}));}
async function analyse(page,file){await page.goto('/observe.html');await page.locator('#detail').setInputFiles(file);await expect(page.locator('#analyse')).toBeEnabled();await page.locator('#analyse').click();await page.waitForURL(/result\.html\?id=/,{timeout:180000});return page.evaluate(()=>new Promise((resolve,reject)=>{const request=indexedDB.open('herbarium.local.v1');request.onsuccess=()=>{const db=request.result,q=db.transaction('observations','readonly').objectStore('observations').getAll();q.onsuccess=()=>{resolve((q.result||[]).at(-1)||null);db.close();};q.onerror=()=>reject(q.error);};request.onerror=()=>reject(request.error);}));}
test.describe('P0-B real species runtime',()=>{test.setTimeout(300000);test('PlantNet + BioCLIP execute without forcing VERIFIED',async({page})=>{await clearDb(page);const item=await analyse(page,sunflower);expect(item).toBeTruthy();expect(item.status).not.toBe('VERIFIED');expect(item.analysis?.automaticGate?.status).not.toBe('REJECT');expect(item.analysis?.speciesEngine?.version).toBe('plantnet-bioclip-consensus-v5');expect(item.analysis?.speciesEngine?.status).not.toBe('UNAVAILABLE');expect(item.analysis?.speciesEngine?.calibrated).toBe(false);expect(Number.isFinite(item.analysis?.speciesEngine?.rawScore)).toBe(true);if(item.status==='PROPOSED'){expect(typeof item.scientificName).toBe('string');expect(item.scientificName.length).toBeGreaterThan(3);expect(item.analysis?.speciesEngine?.consensus===true||item.analysis?.speciesEngine?.arbitrated===true).toBe(true);expect(item.identification?.confidence).toBeNull();expect(item.identification?.calibrated).toBe(false);}});});
